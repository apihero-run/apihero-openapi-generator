import { OpenAPIV2 } from "openapi-types";
import { Service } from "../../@types";

export function getServices(doc: OpenAPIV2.Document): Service[] {
  const services: Service[] = [];

  return services;
}
